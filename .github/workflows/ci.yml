name: Continuous Integration and Deployment

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Set up Python 3.9
        uses: actions/setup-python@v2
        with:
          python-version: 3.9
      - name: Install dependencies
        run: |
          pip install pipenv
          cd backend && pipenv install --deploy --ignore-pipfile
      - name: Run tests
        run: |
          cd backend && pipenv run python manage.py test
      # Add more steps for Docker build, push to registry, and deploy to server or cloud provider
