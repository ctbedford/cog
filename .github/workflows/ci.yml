name: Continuous Integration and Deployment

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Set up Python 3.9
        uses: actions/setup-python@v2
        with:
          python-version: 3.9

      - name: Install dependencies
        run: |
          python -m venv venv
          source venv/bin/activate
          pip install -r backlog/requirements.txt

      - name: Run tests
        run: |
          source venv/bin/activate
          python manage.py test

      # Add steps for building Docker images, pushing to registry,
      # and deploying to your server on cloud provider here.
