name: Continuous Integration and Deployment

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Set up Python 3.9
        uses: actions/setup-python@v2
        with:
          python-version: 3.9

      - name: Install dependencies
        run: |
          pip install pipenv
          cd backend && pipenv install --deploy --ignore-pipfile

      - name: Run tests
        run: |
          cd backend && pipenv run python manage.py test

    # Add steps for building Docker images, pushing to registry,
    # and deploying to your server or cloud provider here.
